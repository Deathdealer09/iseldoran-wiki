# Optional — enables the live LLM-powered AI booking assistant.
# Without it, the assistant falls back to a fast rule-based engine (still fully functional).
ANTHROPIC_API_KEY=

# Model used for the assistant when a key is present.
ANTHROPIC_MODEL=claude-3-5-haiku-latest
